 
import dashboard from "./dashboard"; 
import userManagement from "./userManagement"; 

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [
    dashboard,
    userManagement,
    
    // productManagement,
    // cmsManagement,
    // userProductView,
  ],
};

export default menuItems;
